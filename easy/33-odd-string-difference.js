const { log } = require('console')

/**
 * 差值数组不同的字符串
 * https://leetcode.cn/problems/odd-string-difference/
 * @param {string[]} words
 * @return {string}
 **/
var oddString = function (words) {
  // const obj = {}
  // new Array(26).fill(0).map((i, idx) => {
  //   obj[String.fromCharCode(97 + idx)] = idx
  // })
}

const words = ['aaa', 'bob', 'ccc', 'ddd'] // 'bob'

console.log(oddString(words))
